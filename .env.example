# ========================================
# 🔐 AUTH TEMPLATE - Variables d'environnement
# ========================================

# ----------------------------------------
# 🗄️  DATABASE (PostgreSQL)
# ----------------------------------------
# URL de connexion à votre base de données PostgreSQL
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL="postgresql://postgres:password@localhost:5432/mydb"

# ----------------------------------------
# 🔑 BETTER AUTH
# ----------------------------------------
# URL de base de votre application
# En développement: http://localhost:3000
# En production: https://votredomaine.com
BETTER_AUTH_URL="http://localhost:3000"

# Secret pour signer les sessions (générer avec: openssl rand -base64 32)
BETTER_AUTH_SECRET="votre-secret-super-securise-a-generer"

# ----------------------------------------
# 📧 EMAILS (Resend + React Email)
# ----------------------------------------
# Active/désactive l'envoi réel d'emails
# true = Utilise Resend (nécessite RESEND_API_KEY)
# false = Mode mock (affiche les emails dans la console)
USE_REAL_EMAILS="false"

# Clé API Resend (optionnel si USE_REAL_EMAILS=false)
# Gratuit sur https://resend.com
# RESEND_API_KEY="re_..."

# Email d'expédition (optionnel)
# Par défaut: onboarding@resend.dev
# FROM_EMAIL="noreply@votredomaine.com"

# ----------------------------------------
# 🌐 NEXT.JS (optionnel)
# ----------------------------------------
# URL publique de l'application (pour les emails, callbacks, etc.)
# NEXT_PUBLIC_APP_URL="http://localhost:3000"

# ----------------------------------------
# 📝 NOTES
# ----------------------------------------
# 1. Copier ce fichier vers .env :
#    cp .env.example .env
#
# 2. Remplir les valeurs selon votre environnement
#
# 3. Ne JAMAIS commit le fichier .env (déjà dans .gitignore)
#
# 4. En production, configurez ces variables dans votre plateforme :
#    - Vercel: Project Settings > Environment Variables
#    - Netlify: Site settings > Environment variables
#    - Railway/Render: Environment Variables tab
